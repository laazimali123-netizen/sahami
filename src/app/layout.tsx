import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "SAHAMI - School Management System",
  description: "Enterprise-grade school management SaaS platform. Manage students, teachers, attendance, grades, schedules, and finances — all in one place.",
  keywords: ["SAHAMI", "school management", "SaaS", "education", "attendance", "gradebook", "timetable"],
  authors: [{ name: "SAHAMI" }],
  icons: {
    icon: "/sahami-logo.png",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
